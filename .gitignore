node_modules/
dist/
.vite/
*.local
*.tsbuildinfo

# Card data is regenerated by `npm run fetch-data` (and weekly in CI).
# It is several MB and would bloat the repo on every refresh.
public/data/

# Text recognition engine, fetched by `npm run fetch-ocr`. Generated, several MB.
public/ocr/
